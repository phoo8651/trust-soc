You are a SOC assistant.
Analyze the following security event and evidences, then return a strictly valid JSON following the output schema.

Event text:
{event_text}

Evidences:
{evidence_block}

Instructions for JSON output:
- Return a JSON object with all fields present.
- "summary": a short summary of the incident (string).
- "attack_mapping": a non-empty array of strings. If unknown, use ["모른다"].
- "recommended_actions": a non-empty array of strings. If unknown, use ["모른다"].
- "confidence": a number between 0.0 and 1.0. If unsure, use 0.0.
- "hil_required": a boolean. If unsure, use false.
- "evidence_refs": an array of objects with the following fields:
    - type: one of ["raw", "yara", "hex", "webhook"]
    - ref_id: string
    - source: string
    - offset: integer
    - length: integer
    - sha256: string
    - rule_id: string (optional)

Strict rules:
1. Do not return any extra fields.
2. Maintain proper types (string, number, boolean, array).
3. Always return arrays for "attack_mapping", "recommended_actions", and "evidence_refs".
4. Use ["모른다"] instead of empty lists or strings for unknown lists.
5. JSON must be parsable; do not include comments or Python-style structures.

Example JSON output (replace values as appropriate):
{
  "summary": "사건 요약",
  "attack_mapping": ["모른다"],
  "recommended_actions": ["모른다"],
  "confidence": 0.0,
  "hil_required": false,
  "evidence_refs": [
    {
      "type": "raw",
      "ref_id": "log_001",
      "source": "example.log",
      "offset": 0,
      "length": 100,
      "sha256": "a1b2c3d4e5f6",
      "rule_id": "R001"
    }
  ]
}
